import React, { useState} from 'react'
import { Modal , Button, InputGroup,  FormControl, Table} from 'react-bootstrap'

export function AddMoney(props) {
  return (
    <Button className='col-sm m-2'>Agregar efectivo</Button>
  )
}
